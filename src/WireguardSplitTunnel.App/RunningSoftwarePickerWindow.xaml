<Window x:Class="WireguardSplitTunnel.App.RunningSoftwarePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pick Running App"
        Height="520"
        Width="840"
        MinHeight="420"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#F6F8FC">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Pick Running App"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#1F2937" />
            <TextBlock Margin="0,4,0,0"
                       Foreground="#46607A"
                       Text="Choose running apps with readable .exe paths. Apps without executable paths are hidden." />
        </StackPanel>

        <TextBox x:Name="SearchBox"
                 Grid.Row="1"
                 Margin="0,12,0,10"
                 Padding="8"
                 MinHeight="34"
                 VerticalContentAlignment="Center"
                 TextChanged="OnSearchTextChanged" />

        <DataGrid x:Name="CandidatesGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  RowHeaderWidth="0"
                  SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Add"
                                        Binding="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Width="70" />
                <DataGridTextColumn Header="Process"
                                    Binding="{Binding ProcessName}"
                                    IsReadOnly="True"
                                    Width="180" />
                <DataGridTextColumn Header="Path"
                                    Binding="{Binding ShortPath}"
                                    IsReadOnly="True"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       VerticalAlignment="Center"
                       Foreground="#46607A" />
            <Button Grid.Column="1"
                    Content="Add Selected"
                    MinWidth="120"
                    Padding="10,6"
                    Margin="0,0,8,0"
                    Click="OnAddSelectedClicked" />
            <Button Grid.Column="2"
                    Content="Cancel"
                    MinWidth="90"
                    Padding="10,6"
                    Click="OnCancelClicked" />
        </Grid>
    </Grid>
</Window>
